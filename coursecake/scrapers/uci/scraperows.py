from ..course import Course

class UCIScrapeRows:
    def __init__(self, rows: list):
        self.rows = rows
        self.position = 0


    def scrapeCells(self, row):




    def scrape(self) -> dict:
        '''
        Scrape rows and return dict of courses
        '''
        for row in rows:
            self.scrapeCells(self, row)

            self.position += 1
